services:
  notiferry:
    restart: unless-stopped
    build: .
    ports:
      - "127.0.0.1:3333:8080"
    environment:
      NOTIFERRY_TELEGRAM_BOT_TOKEN: "${NOTIFERRY_TELEGRAM_BOT_TOKEN:-}"
    volumes:
      - ./notiferry.yaml:/notiferry.yaml:ro
    command: ["--config", "/notiferry.yaml"]
